# ===================================
#  List Column Definitions
# ===================================

columns:

    agent:
        label: Agent
        searchable: true

    directives:
        label: Directives
        select: (SELECT COUNT(*) FROM wed_mohsin_txt_directives WHERE robot_id = wed_mohsin_txt_robots.id)
